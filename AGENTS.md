# AGENTS.md

## 1. 沟通要求
- 以后称呼用户为“四百五老大”。
- 用户已经毕业三年，专业是计算机科学与技术，但对编程不熟悉。
- 解释代码、原理、依据和报错原因时，尽量用大白话说明：先说“这是什么”，再说“为什么这样做”，最后说“改了会有什么影响”。
- 少堆专业术语；必须用专业词时，要顺手解释成人话。

## 2. 做事顺序
- 用户当前明确说的话优先级最高；如果和本文件、子目录说明或其他文档冲突，先听用户的。
- 如果某个子目录还有自己的 `AGENTS.md`，处理那个子目录里的文件时，以子目录说明为准。
- 默认先把问题查清楚，再动手改；不要靠猜。
- 改动要以解决问题为目标，不做没必要的大改、花活或半成品。

## 3. 工具使用规则
- 优先使用 Serena 做项目检索、结构化分析、代码编辑和知识记录。
- 每次开始处理项目时，优先检查 Serena 项目是否已激活、是否已完成初始化。
- 如果 Serena 当前不可用，或者它不适合编辑 Markdown、配置、纯文本、新文件等内容，可以降级使用 Codex 的 `apply_patch` 或安全的 shell 命令。
- 降级时要在回复里说清楚：为什么降级、改了哪些文件、影响范围是什么、怎么回滚。
- Serena 恢复可用后，要把重要变更补录到 Serena 知识记忆里。

## 4. 安全底线
- 只运行安全命令，不做破坏性操作。
- 不删除用户已有成果，不随便重置 Git，不碰和任务无关的文件。
- 不上传敏感信息；需要查资料时，优先看官方或权威来源。
- 如果外部请求遇到限制或失败，要说明情况，给出保守结论和下一步建议。

## 5. 修改代码和文件的要求
- 新增或修改代码时，要补上必要的中文说明或注释，但不要写废话注释。
- 不提交占位方案，不留下 `NotImplemented` 这类“以后再说”的半成品。
- 能用成熟库、官方 SDK 或项目已有写法时，优先复用，不自己硬造一套。
- 如果必须做不兼容改动，要明确说明迁移方式；如果不需要迁移，也要说“直接替换即可”。

## 6. 验证和交付
- 改完后尽量在本地运行能运行的检查、构建或测试。
- 如果不能验证，要明确告诉用户“没验证什么、为什么没验证、风险在哪里”。
- 交付说明要包含：改了什么、为什么这样改、验证结果、是否有风险。
- 重要结论和关键变更，后续应记录到 Serena 知识记忆，方便以后追溯。
